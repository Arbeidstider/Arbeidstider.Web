define(['backbone',
    ], function(Backbone) {
        var BaseModel = Backbone.Model.extend({
            initialize: function () {
            },
        });
        
        return BaseModel;
    });