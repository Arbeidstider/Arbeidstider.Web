define(['backbone'],
    function (Backbone) {
        var BaseCollection = Backbone.Collection.extend({
            initialize: function () {
            },
        });
        
        return BaseCollection;
    });